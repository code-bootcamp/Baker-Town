import * as S from "./Navigation.styles";
import { INavigationProps } from "./Navigation.types";

const NavigationPresenter = (props: INavigationProps) => {
  return (
    <>
      <S.Wrapper>네비게이션 영역</S.Wrapper>
    </>
  );
};

export default NavigationPresenter;
