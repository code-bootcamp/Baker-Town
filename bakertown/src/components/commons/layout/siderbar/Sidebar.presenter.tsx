import * as S from "./Sidebar.styles";
import { ISidebarProps } from "./Sidebar.types";

const SidebarPresenter = (props: ISidebarProps) => {
  return (
    <>
      <S.Wrapper>사이드바</S.Wrapper>
    </>
  );
};

export default SidebarPresenter;
